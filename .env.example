# Gallery — optional env vars (copy to .env; never commit .env)
#
# YouTube playlist auto-sync (recommended): required for mode "playlist" or "channel"
YOUTUBE_API_KEY=

# Instagram (phase 2): uncomment when instagram.enabled is true in gallery-config.json
# INSTAGRAM_ACCESS_TOKEN=
# INSTAGRAM_USER_ID=
